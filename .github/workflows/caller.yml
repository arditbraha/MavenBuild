name: Reusable Workflow Caller

on:
  push:
  

jobs:
  Reusable-caller-job:
    uses: ./.github/workflows/maven-cache.yml
    secrets:
      username: ${{ secrets.DOCKERHUB1_USERNAME }}
      password: ${{ secrets.DOCKERHUB1_TOKEN }}
